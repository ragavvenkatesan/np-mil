The data when loaded on matlab contains a structure bag which has 1XN bags and each bag has an array of features where each vector in the array is a feature.
The array labels contain all the label. 
The variable p if provided is the ground truth number of positive prototypes in the instance space. 
